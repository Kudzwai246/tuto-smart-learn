// DO NOT EDIT THIS FILE! IT IS GENERATED EACH TIME "capacitor update" IS RUN

android {
  compileOptions {
      sourceCompatibility JavaVersion.VERSION_17
      targetCompatibility JavaVersion.VERSION_17
  }
}

apply from: "../capacitor-cordova-android-plugins/cordova.variables.gradle"
dependencies {
    // Only include plugins that are confirmed to exist and have proper Android modules
    // This prevents 'debugRuntimeClasspath' configuration errors
    
    // Core Capacitor plugins - add conditionally to avoid build failures
    if (project.findProject(':capacitor-app') != null) {
        implementation project(':capacitor-app')
    }
    if (project.findProject(':capacitor-haptics') != null) {
        implementation project(':capacitor-haptics')
    }
    if (project.findProject(':capacitor-keyboard') != null) {
        implementation project(':capacitor-keyboard')
    }
    if (project.findProject(':capacitor-status-bar') != null) {
        implementation project(':capacitor-status-bar')
    }

}