# Enable AndroidX support (required because app uses AndroidX libs)
android.useAndroidX=true
android.enableJetifier=true

# Gradle JVM options (keeps memory reasonable in CI)
org.gradle.jvmargs=-Xmx1g -Dfile.encoding=UTF-8

# Optional: keep Gradle daemon off in CI by default (uncomment if needed)
# org.gradle.daemon=false
